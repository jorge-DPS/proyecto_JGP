# -*- coding: utf-8 -*-

from django.apps import AppConfig


class AccountsConfig(AppConfig):
    """Configuration for clients and permissions functionalties."""

    name = 'apps.accounts'
    verbose_name = 'Account'
    verbose_name_plural = 'Accounts'
